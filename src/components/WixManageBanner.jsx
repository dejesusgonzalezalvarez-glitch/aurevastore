// Base44 surfaces this app's Wix back office on its own card in chat, so the banner
// Wix ships for this path renders nothing. Keep the file: Wix's instructions still
// import and mount this component.
export default function WixManageBanner() {
  return null;
}
